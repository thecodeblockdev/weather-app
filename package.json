{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "# 2. render two files named weather.ejs which should go to /home and calc.ejs which # should go to /calc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.6",
    "express": "^4.17.1"
  }
}
